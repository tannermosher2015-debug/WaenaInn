# Waena Inn — environment variables
#
# The current site (browse + content) needs NONE of these to run or deploy.
# They are placeholders for the upcoming Booking & Payments phase (Plan 2):
# Stripe checkout + Resend host-notification email.

# Stripe (use TEST keys until the client's real account is connected at handoff)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email notifications (Resend)
RESEND_API_KEY=
HOST_EMAIL=uncletonyshale@gmail.com

# Booking math (confirm real values with the client)
TAX_RATE=0.1775
CLEANING_FEE=75
NEXT_PUBLIC_TAX_RATE=0.1775
